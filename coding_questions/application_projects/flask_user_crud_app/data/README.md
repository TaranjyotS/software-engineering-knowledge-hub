# Runtime Data

Running the application creates `users.db` in this directory. SQLite databases
are generated local state and are excluded by the repository `.gitignore`.

Tests use temporary databases and never modify this directory.
